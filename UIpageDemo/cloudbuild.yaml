steps:
#Build the image 
- name: 'gcr.io/cloud-builders/docker'
  args: ['build','-t','us-central1-docker.pkg.dev/endless-gamma-333908/cicd-repo/${_CONTAINERNAME}:${_VERSION}','.']

#Push the image  
- name: 'gcr.io/cloud-builders/docker'
  args: ['push','us-central1-docker.pkg.dev/endless-gamma-333908/cicd-repo/${_CONTAINERNAME}:${_VERSION}']

 options:
 logging: CLOUD_LOGGING_ONLY
